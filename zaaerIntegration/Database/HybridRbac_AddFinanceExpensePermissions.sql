/*
  DEPRECATED — expense permissions are in HybridRbac_SeedPermissions.sql (finance.expense.*).
  Re-run that file on Master DB instead of this script.
*/
PRINT N'Use HybridRbac_SeedPermissions.sql — finance.expense.* is included there.';
